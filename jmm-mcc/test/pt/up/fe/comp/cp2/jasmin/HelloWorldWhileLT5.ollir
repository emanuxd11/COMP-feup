import ioPlus;

HelloWorld extends Object {

    .method public static main(args.array.String).V {
        i.i32 :=.i32 0.i32;
        goto while_cond_0;
        while_body_0:
        invokestatic(ioPlus, "printHelloWorld").V;
        tmp2.i32 :=.i32 i.i32 +.i32 1.i32;
        i.i32 :=.i32 tmp2.i32;
        while_cond_0:
        tmp0.bool :=.bool i.i32 <.bool 5.i32;
        if (tmp0.bool) goto while_body_0;
        ret.V ;
    }

    .construct HelloWorld().V {
        invokespecial(this, "").V;
    }
}
